# 5. Forms

## Create Task Form&#x20;

we import TextInput from bluebase components.

```
import { JsonFormProps, JsonLayoutProps } from '@bluebase/plugin-json-schema-components';
import React, { useCallback } from 'react';
import { View, ViewStyle } from 'react-native';
import { getComponent, useNavigation, useStyles, useTheme } from '@bluebase/core';

export interface CreateTaskScreenStyles {
	root: ViewStyle;
	avatar: ViewStyle;
}
const JsonForm = getComponent<JsonFormProps<{ title: string; description: string }>>('JsonForm');
const JsonLayout = getComponent<JsonLayoutProps>('JsonLayout');

export const CreateTaskScreen = () => {
	const { push, navigate } = useNavigation();
	const { theme } = useTheme();

	const styles: CreateTaskScreenStyles = useStyles(
		'MyPluginHomeScreen',
		{},
		{
			root: {
				padding: theme.spacing.unit * 2,
			},
			avatar: {
				backgroundColor: theme.palette.warning.main,
			},
		}
	);

	const goToFirst = useCallback(() => {
		push('CreateTask');
	}, [push]);

	const goToHome = useCallback(() => {
		navigate('Home');
	}, [navigate]);

	return (
		<>
			<View>
				<JsonForm
					schema={{
						fields: [
							{
								label: 'Title',
								name: 'title',
								type: 'text',
								placeholder: 'Enter Title',
								helperText: 'helper title',
							},

							{
								label: 'Description',
								name: 'description',
								type: 'text',
							},
							{
								title: 'Save',
								name: 'submit',
								type: 'submit',
							},
						],
						initialValues: { title: '', description: '' },
						onSubmit: (values) => console.log('form submitted', values),
					}}
				/>
			</View>
			<View>
				{/* <JsonLayout
					schema={{
						component: 'View',
						props: {
							style: {
								backgroundColor: 'red',
							},
						},
						children: [
							{
								//JsonLayout
								component: 'H2',
								text: 'Dummy ',
								props: {
									style: {
										color: 'white',
									},
								},
							},
						],
					}}
				/> */}
			</View>
		</>
	);
};

CreateTaskScreen.displayName = 'CreateTaskScreen';

```

&#x20;                                      ![](<../.gitbook/assets/SS (1).png>)
